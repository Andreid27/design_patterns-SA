{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "maven: package (skip tests)",
      "type": "shell",
      "command": "mvn -DskipTests package",
      "options": {
        "cwd": "${workspaceFolder}/backend"
      },
      "problemMatcher": [],
      "group": "build"
    }
  ]
}
